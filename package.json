{
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "lib:dev": "pnpm --prefix $npm_package_config_libdir dev",
    "lib:build": "pnpm --prefix $npm_package_config_libdir build",
    "lib:test": "pnpm --prefix $npm_package_config_libdir test",
    "sample:dev": "pnpm --prefix $npm_package_config_dir dev",
    "sample:build": "pnpm --prefix $npm_package_config_dir build"
  },
  "config": {
    "dir": "./samples/basic",
    "libdir": "./packages/lh99"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "vitepress": "^1.3.4"
  }
}
